/*
After creating an element , we insert that also.
    let newDiv document.createElement('div');

    newDiv.innerText = "Hi , I am new Div";

For inserting we have append function.
    appendChild()
    insertBefore()Like if we need to insert a element inside the body tag at last..

    append();
        used to append more characters
        append as an child.

    prepend();

jis obj ke relative kr rhe hi
    obj.insertAdjacent(position , element);

        position:-
            beforebegin
            afterbegin
            beforeend --> khatam hone se pehle. 
            afterend --> khatam hone ke bad.

*/


/*
    par.removeChild(btn)

    obj.remove()
*/