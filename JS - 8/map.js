let num = [1 , 2, 3, 4, 5];

// new created array always would be same size as num.
let double = num.map( (el) => el*2 ); // returning something in one line even if we not used return statement. 
console.log(double);