// ! Restructuring listings

// *router = express.Router{(mergeParams : true )}
// to use app.js common parent route parameters in router files

// parent route --> common route in app.js , 
// child param --> different route in router.